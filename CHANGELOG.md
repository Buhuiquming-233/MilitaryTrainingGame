# 更新日志

## v1.5（2026-09-07）单文件合集界面与反应速度优化

### 修改
- 单文件版 `军训小游戏合集.html`：进入游戏后的二级界面不再额外显示外层“总菜单/重新开始”工具条，只保留游戏自身的返回与重开按钮，避免出现两层返回键。
- 单文件版游戏视图改为全屏嵌入；点击游戏内“← 主页”仍会返回单文件版总菜单。
- 《听令而动》：缩短正确/错误/超时后的反馈锁定时间（约缩短一半），并提高计时刷新频率，按键后的计分与下一道口令切换更跟手。
- 重新生成单文件版，确保其与各独立游戏页保持同步。

### 自检
- 浏览器实测：二级界面工具栏数量为 1，游戏内返回可回到单文件总菜单；听令而动按键后计分立即变化，无脚本错误。

## v1.4（2026-09-07）新增单文件合集

### 新增
- 新增 `军训小游戏合集.html`：将 7 款游戏与公共样式/脚本全部合并进一个自包含 HTML（约 239 KB），不依赖 `css/style.css`、`js/common.js` 或其他游戏页面。
- 单文件版自带总菜单：7 张游戏卡片点击后在同一文件内进入对应游戏；顶部“返回主页/总菜单”与“重新开始”按钮真实可用。
- 游戏页内原有的“返回主页”也被接管为返回单文件版总菜单。
- `index.html` 增加“单文件合集”卡片，可从普通主界面直接跳转打开。

### 说明
- 单文件版使用隔离的游戏视图呈现各游戏，游戏间互不干扰；全部内容离线内联，可直接双击打开。
- 已实测：总菜单→进入游戏→游玩→返回总菜单→切换另一款→外层与游戏内重新开始均正常，无脚本错误。

## v1.3（2026-09-07）手雷命中后无法继续投掷修复

### 修复
- 《手雷抛物线》：命中判定位于动画循环内部时提前结束循环，导致爆炸画面不渲染、手雷不消失、动画帧彻底停住，第二颗无法投掷。
  - 改为命中后照常绘制当前帧并继续调度动画循环；
  - 结算时先把“清理爆炸并复位手雷”的定时任务排定，再做音效与提示，任何提示环节异常都不会让手雷卡死；
  - 清理后手雷回到手中、可正常投掷下一颗。

### 自检
- 浏览器实测：第一颗命中（+10）后爆炸消失并提示“继续投掷，剩余 4 枚”，第二颗可正常投出并继续结算，全程无脚本错误。

## v1.2（2026-09-07）扫雷显示与手雷判定重做

### 修复
- 《工兵排雷》：翻开格子后未执行画面刷新，导致数字要等标旗/右键后才显示；现在每次翻开都会立即绘制数字与颜色。
- 《手雷抛物线》：删除“进入扩大命中区就自动爆炸”的旧逻辑，改为真实触碰判定——只有碰到靶心（中心区）立即爆炸；仅擦过靶区时不会提前自爆。

### 调整
- 手雷弹道预览恢复显示，但只绘制完整弹道的前 50%（落点与后段仍保密），瞄准时保留角度与力度提示。
- 得分规则改为：穿过靶心直接爆炸得 10 分；未碰到靶心时，按整条飞行路径中最接近靶心的一处“触碰点”计分（10/8/6/4/2 分随接近程度递减），之后在该最接近点引爆。
- 判定使用“线段到靶心最短距离”，避免雷高速穿过靶心时因逐帧取样漏判。
- 游戏内玩法说明同步更新。

### 自检
- 浏览器实测：扫雷翻开瞬间即出现数字，无需标旗刷新；手雷投掷结算、50% 弹道预览均正常且无脚本报错。

## v1.1（2026-09-07）体验修复与手雷新增内容

### 修复
- 修复《工兵排雷》：选择难度后棋盘被页面弹性布局压缩成零宽度、看似“没有扫雷界面”的问题；现在棋盘会撑满可用宽度并自动滚动到可视位置。
- 修复《听令而动》：「向后转」指令的判定键与方向键/屏幕“下”按钮不一致，导致正口令下按 S / ↓ 始终判错；现统一为按 S、↓、下按钮即可正确执行，反口令时按 W / ↑。
- 《边境巡逻蛇》初始与提速后的速度整体放慢约 10%，最低限速从 60ms/步提高到 70ms/步，长蛇阶段也更易操控。

### 新增 / 调整（手雷抛物线）
- 不再显示完整抛物线预测：瞄准时只保留绿色方向线、箭头与力度条，需要玩家根据角度、力度与风力自行判断弹道。
- 靶标出现位置可在每关基准点附近随机上下左右偏移，并随关卡提高偏移范围。
- 关卡由 5 关扩展为 7 关：新增第 6 关“左右移动靶”（靶标按正弦规律左右往返）与第 7 关“环绕移动靶”（靶标按椭圆规律上下左右画圈移动）。
- 靶心判定改为“飞行路径贴近/穿过靶标即命中”，并将可视靶环外缘纳入边缘得分，避免只差一点却被判 0 分。
- 页面提示、HUD 靶型标识与结算文案同步更新。

### 自检
- 浏览器自动化实测：S 键在“向后转”正口令下可正确 +10；扫雷选择难度后棋盘正常显示并可翻开；手雷第 1 关可用常规角度/力度命中得分；蛇类速度参数生效。

## v1.0（2026-09-07）首版发布

### 本次内容
- 全新创建离线可玩的《军训小游戏合集》，共 8 个 HTML 页面：
  - `index.html`：主页，7 张游戏入口卡片，卡片说明含操作方式。
  - `game-listen.html`：《听令而动》四方向反应游戏。
  - `game-boxing.html`：《军体拳速打》连击输入游戏。
  - `game-grenade.html`：《手雷抛物线》蓄力投掷游戏。
  - `game-memory.html`：《装备对号》记忆翻牌配对游戏。
  - `game-sweep.html`：《工兵排雷》扫雷改编游戏。
  - `game-snake.html`：《边境巡逻蛇》贪吃蛇改编游戏。
  - `game-roundup.html`：《围捕敌特》围堵棋游戏。
- 新增共享样式 `css/style.css`：军绿/沙色/米白配色、圆角卡片、通用按钮、弹窗、方向键区、状态条。
- 新增公共脚本 `js/common.js`：音效总开关（默认关闭、Web Audio 本地生成）、最高分与偏好记忆、浮动提示。
- 新增 `CHANGELOG.md`：记录每次迭代。

### 各游戏功能与修复说明
- 《听令而动》：60 秒倒计时，2.8 秒/口令，正确 +10、错误/超时 −5；剩余 30 秒自动进入反口令（左↔右、后→前）；支持方向键 / WASD 与四方向大按钮。
- 《军体拳速打》：随机生成 3~7 步招式套路，方向键 / WASD / 滑动 / 方向按钮输入；正确 +5，完整打完加连击分，错误或超时中断连击并更换套路；结算展示本局打出招式名。
- 《手雷抛物线》：按住拖拽瞄准并蓄力，虚线显示弹道预测；同心圆计分（靶心 10 分），5 关递进、带风力指示与方向箭头；5 枚手雷未达标会失败并允许重开当前关。
- 《装备对号》：4×4 与 6×6 两档难度，8/18 对军品卡牌；计时、翻牌次数、步数与难度纪录均实时记录，通关弹窗提示是否刷新纪录。
- 《工兵排雷》：初/中/高三档雷区；首次点击永不踩雷；电脑左键翻开、右键标旗，手机可用标雷模式按钮或长按 0.5 秒标旗；含剩余雷数、剩余格数与计时，失败展示完整雷区。
- 《边境巡逻蛇》：边界、壕沟与地雷均会致命；吃补给增长并加速；方向键 / WASD / 滑动 / 方向按钮；支持暂停继续并保存最高分。
- 《围捕敌特》：11×11 营地，四个金色角落为唯一出口；BFS 寻路规则可预期，每回合布 1 个哨卡后敌特移动 1 格；四邻封死即捕获，踩出口逃脱或哨卡耗尽为失败；经测试胜负两种结局均可出现。

### 工程说明
- 全部页面为 HTML5 规范，使用 `<meta charset="UTF-8">`、`<meta name="viewport">` 与语义化标签，含中文注释。
- 无任何网络请求、无外部字体/图片/库，`file://` 双击即可运行。
- 手机与桌面尺寸均完成适配检查：无横向溢出，主要按钮不小于 44×44px；触屏使用 pointer/touch 兼容事件。
- 自检结论：主页 7 张卡片可进入对应游戏并返回主页；各游戏均可完整“开始—进行—结束—重开”；自动化浏览器测试未发现页面报错或按钮无响应。
